services:
  - type: web
    name: rakshasetu
    env: python
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput
    startCommand: daphne -b 0.0.0.0 -p $PORT Skillsphere.asgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0